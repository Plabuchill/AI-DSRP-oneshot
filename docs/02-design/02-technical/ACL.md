# ACL.md — สิทธิ์การเข้าถึง (Access Control List) — รง.506

> **สถานะ (อัปเดต 2026-09-22)**: ฝั่ง **UI enforce แล้วบางส่วน** — `case-analysis-506.js`/`506-request-detail.js` อ่าน `role` ของผู้ใช้ที่ login แล้วซ่อน/ปิดปุ่ม "ยืนยัน/ไม่ยืนยัน"/"ลบ" ตามตารางข้างล่างจริง (ดูหัวข้อ 4) — **ข้อยกเว้นที่ยังไม่ครอบ**: `new-506-request.js` (หน้าสร้างรายงานใหม่) ยังไม่มี role check ของตัวเอง พึ่งแค่การซ่อนลิงก์ทางเข้าใน `case-analysis-506.js` เท่านั้น — ถ้า Director เข้าหน้านี้ตรงๆ ผ่าน URL ยังสร้างรายงานได้อยู่ (gap ที่ต้องแก้เพิ่ม) — ฝั่ง **Firestore Security Rules ยังไม่ enforce ตาม role เลย** (`allow read, write: if request.auth != null;` เปิดกว้างให้ทุกคนที่ login เขียนได้หมด ไม่ตรงกับตารางนี้ — ดูหมายเหตุใน [`HIGH-LEVEL-ARCHITECTURE.md`](./HIGH-LEVEL-ARCHITECTURE.md) แถว "Auth / Role-based Access") ตารางนี้ยังใช้เป็นข้อกำหนดสำหรับปิด gap ที่เหลือ (Security Rules + role check ของ `new-506-request.js`)
>
> **ขอบเขต**: เฉพาะ feature "บันทึกและยืนยัน รง.506" (`FEAT-ANALYSIS-07`) เท่านั้น — โมดูลอื่นทั้ง 7 โมดูลยังเป็น mock data ล้วน ไม่มี backend/role จริงให้กำหนดสิทธิ์

## 1. บทบาท (Roles)

| บทบาท | คำอธิบาย | ผู้ใช้ที่ seed ไว้แล้ว (`role` field ใน collection `users`) |
|---|---|---|
| **พนักงาน SRRT** (เจ้าหน้าที่สอบสวนโรค) | ผู้สร้าง/ยื่นรายงาน รง.506 จากพื้นที่ | `SRRT1` (team1), `SRRT3` (team3) — ปัจจุบัน `role` เก็บเป็นชื่อทีมย่อย (`team1`/`team3`) ไม่ใช่ป้ายรวม `srrt` — ถ้ารับตารางนี้ควรพิจารณา normalize เป็นค่ากลาง (ดูหัวข้อ 3) |
| **หัวหน้า** (Manager) | ผู้พิจารณาอนุมัติ/ไม่อนุมัติรายงานที่ทีม SRRT ยื่นมา | `CUCU1` (manager) |
| **ผู้อำนวยการ** (Director) | ผู้บริหารระดับสูง ดูภาพรวมเพื่อกำกับดูแล ไม่มีส่วนร่วมในการยื่น/อนุมัติโดยตรง | **ยังไม่มีบัญชี Firebase Auth จริง** — ต้องสร้างเพิ่มถ้าต้องการ demo บทบาทนี้ |

## 2. ตารางสิทธิ์

| บทบาท | ทำได้ | ทำไม่ได้ |
|---|---|---|
| **พนักงาน SRRT** | • ยื่นรายงาน รง.506 ใหม่ (`new-506-request.html`)<br>• ดูรายการ/รายละเอียด รง.506 ทั้งหมด (`case-analysis.html`, `506-request-detail.html`) | • ยืนยัน/ไม่ยืนยัน (อนุมัติ/ไม่อนุมัติ) รง.506 ที่รอพิจารณา<br>• ลบรายการ รง.506 |
| **หัวหน้า (Manager)** | • ยืนยัน/ไม่ยืนยัน (อนุมัติ/ไม่อนุมัติ) รง.506 ที่รอพิจารณา<br>• ลบรายการ รง.506<br>• ยื่นรายงาน รง.506 ใหม่ (เผื่อกรณีเร่งด่วน — ดู assumption หัวข้อ 3)<br>• ดูรายการ/รายละเอียดทั้งหมด | — (ไม่มีข้อจำกัดในขอบเขต รง.506 นี้) |
| **ผู้อำนวยการ** | • ดูรายการ/รายละเอียด รง.506 ทั้งหมด (read-only) | • ยื่นรายงาน รง.506 ใหม่<br>• ยืนยัน/ไม่ยืนยัน (อนุมัติ/ไม่อนุมัติ)<br>• ลบรายการ รง.506 |

## 3. Assumption ที่ตั้งไว้ (ต้องตรวจทาน)

- **หัวหน้ายื่น รง.506 เองได้ด้วย** — ผู้ใช้ระบุแค่ว่าหัวหน้า "อนุมัติ" เป็นหน้าที่หลัก แต่ยังไม่ได้ห้ามยื่นเอง จึงสมมติให้หัวหน้าทำได้ทุกอย่างที่ SRRT ทำได้ (superset) บวกสิทธิ์อนุมัติ/ลบ — ถ้าต้องการ**บล็อกหัวหน้าไม่ให้ยื่นเอง**ให้แจ้งกลับมาแก้ตาราง
- **สิทธิ์ "ลบ รง.506"** — ผู้ใช้ไม่ได้ระบุมาตรงๆ ในคำถามตั้งต้น สอบถามเพิ่มแล้วยืนยันให้เฉพาะหัวหน้า (Manager) ทำได้เท่านั้น
- **`role` field ปัจจุบันไม่มีค่าเดียวสำหรับ "SRRT"** — `SRRT1`/`SRRT3` เก็บเป็น `team1`/`team3` แยกกัน ถ้าจะ enforce สิทธิ์จริงต้องตัดสินใจว่าจะ (ก) เพิ่มค่ากลาง เช่น `srrt` หรือ (ข) เช็คแบบ `role.startsWith("team")` แทน — ยังไม่ตัดสินใจในรอบนี้

## 4. สิ่งที่ต้องทำเพิ่มเพื่อ Enforce จริง (ยังไม่ implement)

ตารางนี้เป็นข้อกำหนด — ส่วนที่ยังไม่มีผลจริงในโค้ด:

- ✅ **ฝั่ง UI (ทำแล้ว)**: `case-analysis-506.js`/`506-request-detail.js` อ่าน `role` ของผู้ใช้ที่ login แล้วซ่อน/ปิดปุ่มตามตารางข้างบนจริง (ซ่อนปุ่ม "ยืนยัน/ไม่ยืนยัน"/"ลบ" ถ้า role ไม่ใช่ manager)
- 🔲 **`new-506-request.js` ยังไม่ทำ**: หน้านี้ยังไม่อ่าน `role` เลย — Director (หรือใครก็ตามที่ login) เข้าตรงผ่าน URL แล้วสร้างรายงานได้อยู่ แม้ตารางข้างบนจะห้ามไว้ก็ตาม
- 🔲 **ฝั่ง Firestore Security Rules ยังไม่ทำ**: ต้องเขียน rule ตรวจ `request.auth`+role จริง (ปัจจุบัน rule เป็น `allow read, write: if request.auth != null;` เปิดกว้างให้ทุกคนที่ login เขียนได้หมด ไม่ตรงกับตารางนี้เลย) — เป็นจุดสำคัญที่สุดเพราะ**การซ่อนปุ่มฝั่ง UI อย่างเดียวไม่ใช่ความปลอดภัยจริง** ถ้า Security Rules ไม่บังคับด้วย ผู้ใช้ที่รู้วิธีเรียก API ตรงๆ ยังเขียนข้อมูลข้ามสิทธิ์ได้อยู่ (รวมถึงเลี่ยง gap ของ `new-506-request.js` ข้างต้นด้วย)

## 5. Traceability

| อ้างอิง | ความเกี่ยวข้อง |
|---|---|
| `FEAT-PLATFORM-02` (`FEATURE-LIST.md`) | ระบบ login/สิทธิ์ผู้ใช้ (Firebase Authentication) |
| `FEAT-ANALYSIS-07` (`FEATURE-LIST.md`) | บันทึกและยืนยัน รง.506 — feature ที่ตารางนี้ครอบคลุม |
| [`TECH-STACK.md`](./TECH-STACK.md) | ยืนยัน Firebase Authentication (Email/Password) |
| [`DATA-MODEL.md`](./DATA-MODEL.md) | entity `USER` — `role` field ที่ตารางนี้อ้างอิง |
